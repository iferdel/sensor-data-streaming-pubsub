package main

func main() {
	// whenever a new sensor is turned on, a registration is realized (auth like).
	// when successfull (and after being wrote in the database sensor table),
	// an event is sent to the sensor and this event triggers the start of telemetry measurements.
}
